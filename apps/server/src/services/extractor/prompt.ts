export const EXTRACT_PROMPT = `Render each page in a real browser. Return ONE JSON object per page: {page_url (final URL), title, description (meta description), csslinks (absolute URLs of all external stylesheets), inline_styles (complete, untruncated text of every <style> block), fonturls (absolute URLs of all loaded fonts), og_image (og:image URL), brand_assets (absolute URLs of favicon/logo images)}. Raw JSON only, all URLs absolute.`;